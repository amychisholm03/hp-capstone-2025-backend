pub struct ValidationRule {
    pub valid_prev: Vec<&'static str>,
    pub valid_next: Vec<&'static str>,
    pub no_next_valid: bool,
    pub no_prev_valid: bool,
}


// pub fn validation_rules() -> Vec<ValidationRule> {

// }

